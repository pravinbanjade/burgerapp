import * as actionTypes form './actions';

const initialState = {
  ingredient : null,
  totalPrice: 30,
}

const reducer = (state = initialState, action) => {

}

export default reducer;
